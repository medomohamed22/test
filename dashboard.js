const token=localStorage.getItem('dw_token')||'', $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(p){const r=await fetch(p,{headers:{Authorization:'Bearer '+token},cache:'no-store'}),b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.error||`HTTP ${r.status}`);return b}function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2200)}
async function init(){if(!token){location.href='/account.html';return}try{const d=await api('/api/dashboard'),s=d.summary;$('#pageContent').innerHTML=`<div class="dash-head"><div><h1>أداء متجرك</h1><p class="muted">راقب ما يجذب المشترين وحسّن إعلاناتك بناءً على البيانات.</p></div><a class="btn primary linkbtn" href="/sell.html">إعلان جديد</a></div><div class="stat-grid"><div class="stat"><span>الإعلانات</span><b>${s.listings}</b></div><div class="stat"><span>المشاهدات</span><b>${s.views}</b></div><div class="stat"><span>المفضلة</span><b>${s.favorites}</b></div><div class="stat"><span>المحادثات</span><b>${s.chats}</b></div><div class="stat"><span>إنفاق الترويج</span><b>${Number(s.revenuePi).toFixed(2)} π</b></div></div><section class="card"><div class="section-head"><h2>أداء الإعلانات</h2><span class="muted">آخر 30 يومًا</span></div><div class="analytics-list">${d.listings.length?d.listings.map(l=>`<a href="/dashboard.html?listing=${l.id}" class="analytics-row"><div><b>${esc(l.title)}</b><small>${esc(l.status)}</small></div><div><b>${l.views30d}</b><small>مشاهدة</small></div><div><b>${l.favorites}</b><small>مفضلة</small></div><div><b>${l.chats}</b><small>محادثة</small></div></a>`).join(''):'<div class="empty">لا توجد إعلانات بعد</div>'}</div></section><section id="detail"></section><section class="card"><div class="section-head"><h2>آخر مدفوعات الترويج</h2><a href="/account.html">الحساب</a></div>${d.payments.length?d.payments.slice(0,8).map(p=>`<div class="payment-row"><b>${esc(p.service)}</b><span>${Number(p.amount_pi).toFixed(2)} π</span><small>${new Date(p.created_at).toLocaleDateString('ar-EG')}</small></div>`).join(''):'<div class="muted">لا توجد مدفوعات بعد.</div>'}</section>`;const id=new URLSearchParams(location.search).get('listing');if(id)loadDetail(id)}catch(e){$('#pageContent').innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
async function loadDetail(id){try{const d=await api('/api/listings/'+encodeURIComponent(id)+'/promotion-analytics');const vals=Object.entries(d.dailyViews),max=Math.max(1,...vals.map(x=>x[1]));$('#detail').innerHTML=`<section class="card"><div class="section-head"><div><h2>${esc(d.listing.title)}</h2><span class="muted">تحليل الترويج والتفاعل</span></div><a class="btn primary linkbtn" href="/promote.html?id=${id}">تمييز الإعلان</a></div><div class="mini-stats"><span><b>${d.views30d}</b> مشاهدة/30 يوم</span><span><b>${d.favorites}</b> مفضلة</span><span><b>${d.chats}</b> محادثة</span></div><div class="bar-chart" aria-label="مشاهدات آخر 30 يوم">${vals.length?vals.slice(-14).map(([day,v])=>`<div class="bar-col" title="${day}: ${v}"><i style="height:${Math.max(8,v/max*100)}%"></i><small>${day.slice(5)}</small></div>`).join(''):'<div class="muted">لا توجد مشاهدات حديثة.</div>'}</div></section>`;$('#detail').scrollIntoView({behavior:'smooth'})}catch(e){toast(e.message)}}init();
