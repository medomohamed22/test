'use strict';
const {createClient}=require('@supabase/supabase-js');
const sb=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}});
module.exports=async(req,res)=>{try{if(req.method==='GET'){res.setHeader('Cache-Control','public, s-maxage=30, stale-while-revalidate=120');await sb.from('products').update({promoted_until:null,promotion_tier:null,promoted_level:null,promoted_priority:null}).lt('promoted_until',new Date().toISOString()).eq('status','approved');const {data,error}=await sb.from('products').select('id,seller_pi_id,seller_username,name,description,category,country,location,price_usd,images,status,views,promoted_until,promotion_tier,created_at').eq('status','approved').order('promotion_tier',{ascending:false,nullsFirst:false}).order('promoted_until',{ascending:false,nullsFirst:false}).order('created_at',{ascending:false}).limit(200);if(error)throw error;return res.json({products:data||[]})}if(req.method==='POST'){res.setHeader('Cache-Control','no-store');const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{}),id=Number(b.id);if(!Number.isInteger(id)||id<1)return res.status(400).json({error:'Invalid id'});const {data,error}=await sb.rpc('increment_product_views',{product_id_input:id});if(error){console.error('view count:',error.message);return res.json({views:null,counted:false})}return res.json({views:data,counted:true})}return res.status(405).json({error:'Method not allowed'})}catch(e){console.error('public-products:',e);return res.status(500).json({error:'Request failed'})}};
